name: scopely
description: Enhances Dart's async/await model with structured concurrency utilities.
version: 1.1.2
repository: https://github.com/skylon07/scopely

environment:
  sdk: ^3.7.2

dependencies:
  # should NOT depend on Flutter or Flutter-dependent packages;
  # this package should be usable by pure Dart applications
  # flutter: ...

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
